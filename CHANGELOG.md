# Changelog

## [2.5.0] 2026-04-29

### Added

- Add per-timestamp index.json generation in EWMRS render pipeline listing valid tiles
- Add timestamp tile index lookups to `/renders/tile` endpoint when x/y are omitted
- Add debug port handling with `--debug-server` flag in EWMRS API server
- Add EWMRS RAP endpoints `/rap/layers`, `/rap/mappings`, `/rap/fetch`, `/rap/metadata`, and `/rap/data` for layer discovery, mappings lookup, timestamp index access, metadata retrieval, and Uint16 raster downloads

### Changed


### Fixed

### Testing

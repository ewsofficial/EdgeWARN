schema_version = 1
id = "brokenmanifest"
name = "BrokenManifest"
# Deliberately malformed: 'version' must be a three-part version string, and
# there are no [[writes]] blocks either. Discovery must record this directory as
# invalid with an actionable reason rather than dropping it.
version = "one"
api_version = "1"
entrypoint = ["{python}", "main.py"]

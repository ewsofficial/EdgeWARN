# alerts.yaml — Audit §6
#
# Source: src/EdgeWARN/alerts/schema.py; schedule/scheduler.py; api_integration/index_manager.py; EdgeWARN/pipeline.py
# Values verified against the working tree at commit e12d407.
# Not yet consumed by code; see plans/yaml-configurations.md.

schema_version: 1

alerts:
  default_severity: warning

schedule:
  mrms_update_checker_max_entries: 10

api_index:
  remove_old_cells: true
  resync_every_updates: 500

runtime:
  initialize_indexes:
    realtime: true
    historical: false

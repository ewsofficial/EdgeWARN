{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "filesystem.yaml",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "base_dir", "cleanup_defaults", "scan", "colormaps"],
  "properties": {
    "schema_version": { "const": 1 },
    "base_dir": {
      "type": "object",
      "additionalProperties": false,
      "required": ["windows", "posix", "container_fallback"],
      "properties": {
        "windows": { "type": "string" },
        "posix": { "type": "string" },
        "container_fallback": { "type": "string" }
      }
    },
    "cleanup_defaults": {
      "type": "object",
      "additionalProperties": false,
      "required": ["max_age_minutes", "max_files", "skip_extensions"],
      "properties": {
        "max_age_minutes": { "type": "integer", "minimum": 0 },
        "max_files": { "type": ["integer", "null"], "minimum": 0 },
        "skip_extensions": { "type": "array", "uniqueItems": true, "items": { "type": "string" } }
      }
    },
    "scan": {
      "type": "object",
      "additionalProperties": false,
      "required": ["skip_extensions_always", "skip_extensions_when_base_present"],
      "properties": {
        "skip_extensions_always": { "type": "array", "uniqueItems": true, "items": { "type": "string" } },
        "skip_extensions_when_base_present": { "type": "array", "uniqueItems": true, "items": { "type": "string" } }
      }
    },
    "colormaps": {
      "type": "object",
      "additionalProperties": false,
      "required": ["search_path", "fallback"],
      "properties": {
        "search_path": { "type": "array", "minItems": 1, "uniqueItems": true, "items": { "type": "string" } },
        "fallback": { "type": "string" }
      }
    },
    "env_overrides": {
      "type": "object",
      "additionalProperties": { "type": ["string", "array"], "items": { "type": "string" } }
    }
  }
}

# AGENTS Guide for `src/EdgeWARN/ctam/modules/`

## Purpose
Built-in CTAM modules.

## Agent guidance
- Each module should remain independently testable.
- Avoid introducing hidden coupling between modules unless the framework explicitly supports it.

# AGENTS Guide for `src/EdgeWARN/api/`

## Purpose
Node/Express EdgeWARN API service.

## Agent guidance
- Use existing Express router patterns and ES modules.
- Preserve `helmet`, `cors`, `compression`, and rate limiting behavior.
- Keep public route contracts aligned with `docs/api/`.
